import 'package:flutter/material.dart';

void main() {
  runApp(IfaIslamiApp());
}

class IfaIslamiApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Ifa Islami School Management',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: LoginPage(),
    );
  }
}

// ================= Login Page =================
class LoginPage extends StatelessWidget {
  final TextEditingController emailController = TextEditingController();
  final TextEditingController passwordController = TextEditingController();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Admin Login')),
      body: Padding(
        padding: EdgeInsets.all(20),
        child: Column(
          children: [
            TextField(
              controller: emailController,
              decoration: InputDecoration(labelText: 'Email'),
            ),
            SizedBox(height: 10),
            TextField(
              controller: passwordController,
              decoration: InputDecoration(labelText: 'Password'),
              obscureText: true,
            ),
            SizedBox(height: 20),
            ElevatedButton(
              onPressed: () {
                // TODO: Firebase Auth Login
                Navigator.push(
                    context,
                    MaterialPageRoute(
                        builder: (context) => AdminDashboard()));
              },
              child: Text('Login'),
            ),
          ],
        ),
      ),
    );
  }
}

// ================= Admin Dashboard =================
class AdminDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Admin Dashboard')),
      body: Padding(
        padding: EdgeInsets.all(20),
        child: Column(
          children: [
            ElevatedButton(
              onPressed: () {
                // TODO: Student List Page
              },
              child: Text('Manage Students'),
            ),
            ElevatedButton(
              onPressed: () {
                // TODO: Teacher List Page
              },
              child: Text('Manage Teachers'),
            ),
            ElevatedButton(
              onPressed: () {
                // TODO: Attendance Page
              },
              child: Text('Attendance'),
            ),
            ElevatedButton(
              onPressed: () {
                // TODO: Marks Page
              },
              child: Text('Marks'),
            ),
            ElevatedButton(
              onPressed: () {
                // TODO: SMS System
              },
              child: Text('Send SMS'),
            ),
          ],
        ),
      ),
    );
  }
}
